<%= link_to password, id: dom_id(password), class: "block p-4 no-underline rounded hover:bg-gray-100" do %>
    <div class="text-sm text-gray-500 font-normal"><%= password.url %></div>
    <div><%= password.username %></div>
<% end %>
